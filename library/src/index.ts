export { GraphQLClient } from "graphql-request"

// Telemetry
export * from "./telemetry/index.js"

// Default client bindings
export * from "./api/client.gen.js"

// Common errors
export * from "./common/errors/index.js"

// Connection for library
export type { CallbackFct } from "./connect.js"
export { connect, connection } from "./connect.js"
export type { ConnectOpts } from "./connectOpts.js"

// Export dagger connection context
export { Context, BaseClient } from "./common/context.js"

// Module library
export * from "./module/decorators.js"
// `entrypoint` is deliberately not re-exported: it is the *dynamic* dispatcher,
// which scans a module's source at call time and so pulls the whole
// introspector — and with it the TypeScript compiler — into this bundle. Modules
// generated by this SDK carry a static __dagger.entrypoint.ts instead and never
// call it. See library/VENDOR.md; packager:module-bundle-check keeps it out.
export { getRegisteredClass } from "./module/registry.js"
