# SDK checks and generators run inside the pinned development engine.
# Its workspace configuration is .dagger/modules/engine-e2e/workspace.toml.

[modules.engine-e2e]
source = ".dagger/modules/engine-e2e"
